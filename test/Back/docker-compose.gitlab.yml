version: "3.4"
services:

  test_Back:
    build:
      context: .
      dockerfile: ${DOCKERFILE}
      args:
        REPOSITORY: ${REPOSITORY_PROD}
        PORT: ${test_Back_PORT}
    
    networks:
      - test-network
    


networks:
   test-network:

