version: ${VERSION_COMPOSE}
services:

  test_front:
    build:
      context: .
      args:
        REPOSITORY: ${REPOSITORY}
    
    networks:
      - test-network
    


networks:
   test-network:

